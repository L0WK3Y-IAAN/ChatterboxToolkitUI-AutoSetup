The real test, though, is whether Mal trail is actually seeing traffic. You'll generate a bit of traffic against this host — including a lookup for that demo indicator the script installed earlier — and watch a live detection land in the dashboard. That's your proof the sensor is capturing packets, matching them against known-bad indicators, and the server is logging and displaying the result correctly, end to end.
