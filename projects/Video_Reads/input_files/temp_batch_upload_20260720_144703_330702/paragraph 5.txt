After that comes configuration. This is where the script's role-based logic actually matters: since you chose Both, it skips the remote-reporting setup entirely — the sensor will just write straight into the local log directory instead of reporting over the network to a separate server. A sensor-only install takes a different path here, prompting for a remote server's address instead of writing locally.
