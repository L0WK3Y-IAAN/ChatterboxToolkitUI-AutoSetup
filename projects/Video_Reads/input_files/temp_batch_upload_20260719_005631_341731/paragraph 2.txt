Before touching Mal trail itself, there's one stumbling block worth calling out up front: the packet capture library. Mal trail depends on a library called pcapy, but the original pcapy package is deprecated and causes real problems on modern Python 3 environments. The fix is to install pcapy-ng instead — a maintained fork that's a drop-in replacement. If your install ever fails at the packet capture step, this is almost always why.
